.home
  .banners
    .container
      .row
        .col-md-12
          %h2 Salesforce Connector Overview
          Connectors allow Connec!™ to sync with third-party applications such as CRM systems, Accounting and Payroll softwares. Data is exchanged in real-time so you can better manage your business operations.

  - if current_user
    .container
      .row
        .col-md-12
          Your company #{@organization.name} (#{@organization.uid}) is currently linked

        .col-md-12
          - if @organization.oauth_uid
            Your SalesForce account (#{@organization.oauth_uid}) is currently linked
          - else
            Your SalesForce account is not linked
            = link_to "Link this company to SalesForce", "/auth/salesforce/request?org_uid=#{@organization.uid}", class: "btn btn-warning btn-sm" if is_admin?(current_user, @organization)

  - else
    .container
      .row
        .col-md-4.col-md-offset-4.center
          = link_to "Link your Maestrano account", Maestrano::Connector::Rails::Engine.routes.url_helpers.default_maestrano_auth_saml_index_path(tenant: :default), class: 'btn btn-warning'
